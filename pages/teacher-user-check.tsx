import MyLayout from 'layouts/index';
import Compose from 'inner-page/teacher-user-check/compose';

/**
 * 角色：高校导师
 * 信息审批管理->用户账号审核
 */
const UserCheck = () => {
  return <Compose></Compose>;
};
UserCheck.Layout = MyLayout;

export default UserCheck;
